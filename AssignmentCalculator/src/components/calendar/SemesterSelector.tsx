import {Button} from "@headlessui/react";

export const SemesterSelector = () => {
    return (
        <Button className="rounded-lg bg-sky-600 w-12 h-6">
            butone
        </Button>
    )
}